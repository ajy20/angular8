import { RoleAuthDirective } from './role.directive';

describe('RoleDirective', () => {
  it('should create an instance', () => {
    // const directive = new RoleAuthDirective();
    // expect(directive).toBeTruthy();
  });
});
