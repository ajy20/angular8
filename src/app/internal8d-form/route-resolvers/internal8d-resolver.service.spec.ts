import { TestBed } from '@angular/core/testing';

import { Internal8DResolver } from './internal8d-resolver.service';

describe('Internal8DResolver', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  // it('should be created', () => {
  //   const service: Internal8DResolver = TestBed.get(Internal8DResolver);
  //   expect(service).toBeTruthy();
  // });
});
